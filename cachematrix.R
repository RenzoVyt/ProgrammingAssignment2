## 
## input x as matrix
## set every solved value "a" as null
## changed every "mean" to "solve"
jorscatxxsCachematrix <- function(x = matrix(sample(59:5512,70),92,85) {
        a <- NULL
        metajoksx <- function(y){
                x <<- y
                a <<- NULL
                }
        phantomxs <- function() x 
        metajoksphantomxs <- function(solve) a <<- inverse
        metajoksphantomxs <- function() s 
        list (set = set,
              get = get,
              setmean = setmean,
              getmean = getmean)
        
                        
}


## input x as matrix
## set "a" as null
## change "mean" to "solve"
yoriuscacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
        x <- x$getmean()
        if (!is.null(a)){
                message ("getting the average data")
                return (a)
                }
        data <- x$get()
        a <- solve(data, ...)
        x$setmean(a)
        v
}
